out/*
release/*
node_modules
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*/dist/*
dist/*
.DS_Store
native/**/target
native/target-rust-analyzer
native/**/.build
out.json
*.tsbuildinfo
tsconfig.node.tsbuildinfo
server/deploy-dev.sh

# CDK context cache — regenerable, and pins account/zone/domain identifiers we
# don't need to publish. CDK recreates it locally on synth.
server/infra/cdk.context.json

# Sentry Config File
.env.sentry-build-plugin
.electron-builder-cache
.npm-cache
.cache
